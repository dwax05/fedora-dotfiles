#!/bin/sh

[[ -n $wallpaper_path ]] || wallpaper_path="$HOME/.local/wallpapers/huh"

rm $HOME/.cache/kittywallpaper.png
wallpaper=$(find $wallpaper_path -type f | shuf -n 1)
ln -s "$wallpaper" $HOME/.cache/kittywallpaper.png
